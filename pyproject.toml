[project]
name = "modelretrieval"
version = "0.1.0"
description = "Documentation site for the NTCIR-19 ModelRetrieval benchmark"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["mkdocs-material>=9.7.0"]
